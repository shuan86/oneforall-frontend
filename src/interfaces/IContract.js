export const NewsType = { Unreview: 0, UnderReviewed: 1, Reviewed: 2 };
/*export const NewsTag = {
  sport=0,
  medica=1,
  food=2,
};*/
/*export const getArticleName=(tags)=>{
    const keyNames = Object.keys(NewsTag);
    for(const t of tags){
        let i=0;
       for(k of keyNames){
         if(t==NewsTag[i])
             return keyNames[i];
        }
      i++;
    }
}*/
